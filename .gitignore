# macOS
.DS_Store
._*

# Editor swap / lock files
*.swp
*.swo
*~
.#*

# Editor state — keep .claude/launch.json (dev-server config) but ignore private overrides
.claude/settings.local.json
.claude/*.local.*
.vscode/
.idea/

# Ad-hoc scratch
scratch/
tmp/

# Compiled Mac app bundle (build artifacts) — source lives in mac-app/
mac-app/build/
